# Register Map

<!-- GENERATED FILE - DO NOT EDIT. -->

Generated by `scripts/gen_regmap.py` from `control/regmap.json`, the single source of truth
for the register and control plane ([SPEC.md](../SPEC.md) §9, issue #7). Edit the source
and re-run the generator; `make regmap-check` fails the build on any hand edit here or
on a source change that was never regenerated.

- Register-map version: **1.0.0** (schema 1)
- Interface: 32-bit data, 16-bit byte address, 4 byte enables
- Window per block: `0x1000` bytes
- Blocks declared: 9 (5 implemented), registers implemented: 28

## Access types

| Type | Meaning |
|---|---|
| `RO` | Constant. The reset value is the value. Writes to a register whose fields are all `RO`/`ROHW` return `error=1`. |
| `ROHW` | Read-only, driven by hardware (build parameters, status, counters). |
| `RW` | Read-write storage, byte-enable maskable. |
| `W1C` | Sticky: set by hardware, cleared by writing 1. Writing 0 has no effect; a hardware set beats a simultaneous clear. |
| `RWP` | Write-1-pulse: emits a one-cycle pulse, always reads 0. |

A register with a mix of writable and non-writable fields accepts writes and ignores
the non-writable bits (`error=0`).

## Address map

| Block | Window | Registers | Status | SPEC §9 groups |
|---|---|---|---|---|
| `id` | `0x0000`–`0x0FFF` | 4 | implemented | Global identification |
| `build_params` | `0x1000`–`0x1FFF` | 12 | implemented | Build parameters |
| `ctrl` | `0x2000`–`0x2FFF` | 4 | implemented | Per-block enable and reset |
| `fault` | `0x3000`–`0x3FFF` | 4 | implemented | Fault injection |
| `scratch` | `0x4000`–`0x4FFF` | 4 | implemented | Snapshot and debug control |
| `coeff` | `0x5000`–`0x5FFF` | 0 | planned (#10, #11, #12, #16) | Coefficient and weight programming; Active bank selection |
| `cfar` | `0x6000`–`0x6FFF` | 0 | planned (#14, #16) | CFAR settings; Integration settings |
| `counters` | `0x7000`–`0x7FFF` | 0 | planned (#8) | Stream counters; Stall counters; FIFO high-water marks; Overflow and saturation counts; Frame counts; Sequence errors; CDC errors |
| `debug` | `0x8000`–`0x8FFF` | 0 | planned (#19) | Snapshot and debug control |

Any address outside every implemented window, any address inside a window but beyond
that block's last register, any unaligned address, a write with no byte enables set,
and a simultaneous read and write all complete with `ready=1, error=1` and no side
effect. The fabric never stalls indefinitely: see the response contract in
[DECISIONS.md](../DECISIONS.md).

## `id` — `0x0000`

Fixed identification of the control plane itself. Every field is a constant folded in at elaboration, so a live design can be identified without any prior knowledge of what was built.

| Offset | Address | Register | Access | Reset | Description |
|---|---|---|---|---|---|
| `0x000` | `0x0000` | `MAGIC` | RO | `0x52414441` | Constant marker. Reading 0x52414441 ('RADA') at block base proves a control plane is present and the address decode is alive. |
| `0x004` | `0x0004` | `VERSION` | RO | `0x01000001` | Register-map version, from regmap_version in the source of truth. Static build data, deliberately not a git describe: the same source tree must produce the same register contents on any machine, and a VCS-derived value would make the generated artefacts depend on checkout state. |
| `0x008` | `0x0008` | `GEOMETRY` | RO | `0x10201C09` | Shape of the register plane, so a discovery walk needs no compiled-in constants. |
| `0x00C` | `0x000C` | `CAPABILITY` | RO | `0x0000001F` | One bit per declared block, set when that block is implemented in this build. Bit i is block i in declaration order; a planned block reads 0 and its window returns error. |

### `ID.MAGIC` — `0x0000`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `MAGIC` | RO | `0x52414441` | — | ASCII 'RADA'. |

### `ID.VERSION` — `0x0004`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:24` | `MAJOR` | RO | `0x1` | — | Incompatible layout change. |
| `23:16` | `MINOR` | RO | `0x0` | — | Registers or fields added. |
| `15:8` | `PATCH` | RO | `0x0` | — | Documentation-only change. |
| `7:0` | `SCHEMA` | RO | `0x1` | — | Source-of-truth schema version. |

### `ID.GEOMETRY` — `0x0008`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `7:0` | `N_BLOCKS` | RO | `0x9` | — | Declared blocks, implemented and planned. |
| `15:8` | `N_REGS` | RO | `0x1C` | — | Implemented registers across all blocks. |
| `23:16` | `DATA_W` | RO | `0x20` | — | Register data width in bits. |
| `31:24` | `ADDR_W` | RO | `0x10` | — | Register address width in bits. |

### `ID.CAPABILITY` — `0x000C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `BLOCK_MASK` | RO | `0x1F` | — | Implemented-block bitmap. |

## `build_params` — `0x1000`

Read-only mirror of the elaboration parameters. Every value is driven from config_pkg, which scripts/build_verilator.py generates from config/<name>.json (SPEC 11), so this block reports what was actually elaborated rather than what a header claims.

| Offset | Address | Register | Access | Reset | Description |
|---|---|---|---|---|---|
| `0x000` | `0x1000` | `N_ANTENNAS` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x004` | `0x1004` | `SAMPLES_PER_CYCLE` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x008` | `0x1008` | `FFT_SIZE` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x00C` | `0x100C` | `PFB_TAPS` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x010` | `0x1010` | `N_BEAMS` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x014` | `0x1014` | `HISTORY_FRAMES` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x018` | `0x1018` | `PACKET_W` | ROHW | `0x00000000` | SPEC 11 sized parameter. |
| `0x01C` | `0x101C` | `SAMPLE_W` | ROHW | `0x00000000` | SPEC 3 invariant parameter. |
| `0x020` | `0x1020` | `COEFF_W` | ROHW | `0x00000000` | SPEC 3 invariant parameter. |
| `0x024` | `0x1024` | `POWER_W` | ROHW | `0x00000000` | SPEC 3 invariant parameter. |
| `0x028` | `0x1028` | `N_VIRTUAL_CHANS` | ROHW | `0x00000000` | SPEC 3 invariant parameter. |
| `0x02C` | `0x102C` | `PARAM_CHECKSUM` | ROHW | `0x00000000` | FNV-1a 32 over the eleven parameter words above, in offset order, little-endian bytes. Computed in RTL from config_pkg and independently in the C++ harness from config_sim.h: a mismatch means the RTL and the harness were elaborated from different configurations, which is otherwise a silent and very expensive failure. |

### `BUILD_PARAMS.N_ANTENNAS` — `0x1000`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::N_ANTENNAS` | Antenna count. |

### `BUILD_PARAMS.SAMPLES_PER_CYCLE` — `0x1004`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::SAMPLES_PER_CYCLE` | Samples presented per core clock. |

### `BUILD_PARAMS.FFT_SIZE` — `0x1008`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::FFT_SIZE` | FFT points. |

### `BUILD_PARAMS.PFB_TAPS` — `0x100C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::PFB_TAPS` | Polyphase taps per branch. |

### `BUILD_PARAMS.N_BEAMS` — `0x1010`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::N_BEAMS` | Formed beams. |

### `BUILD_PARAMS.HISTORY_FRAMES` — `0x1014`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::HISTORY_FRAMES` | Frames of history retained. |

### `BUILD_PARAMS.PACKET_W` — `0x1018`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::PACKET_W` | Packet datapath width. |

### `BUILD_PARAMS.SAMPLE_W` — `0x101C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::SAMPLE_W` | Sample component width. |

### `BUILD_PARAMS.COEFF_W` — `0x1020`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::COEFF_W` | Coefficient width. |

### `BUILD_PARAMS.POWER_W` — `0x1024`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::POWER_W` | Power/magnitude accumulator width. |

### `BUILD_PARAMS.N_VIRTUAL_CHANS` — `0x1028`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `config_pkg::N_VIRTUAL_CHANS` | Packet virtual channels. |

### `BUILD_PARAMS.PARAM_CHECKSUM` — `0x102C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | `computed` | FNV-1a 32 of the build parameters. |

## `ctrl` — `0x2000`

Per-block enable and soft-reset stubs. The bit assignment is fixed now so that each kernel issue wires its own enable and reset without renumbering anything; a bit whose consumer has not landed drives an output of control_top and nothing else.

| Offset | Address | Register | Access | Reset | Description |
|---|---|---|---|---|---|
| `0x000` | `0x2000` | `BLOCK_ENABLE` | RW | `0x000000FF` | Per-block enable. Reset value enables every block, so a build with no software present still runs. |
| `0x004` | `0x2004` | `BLOCK_RESET` | RWP | `0x00000000` | Per-block soft reset request. Writing 1 emits a one-cycle pulse in the cfg_clk domain; the bit always reads 0. Crossing the pulse into each block's own domain is issue #6 work and is deliberately not done here. |
| `0x008` | `0x2008` | `GLOBAL_CTRL` | MIXED | `0x00000001` | Datapath-wide control. GLOBAL_ENABLE gates every block regardless of BLOCK_ENABLE. |
| `0x00C` | `0x200C` | `CTRL_STATUS` | ROHW | `0x00000000` | Hardware-driven status. ENABLED_COUNT is computed in the block from the live BLOCK_ENABLE value, so it exercises a real hardware read path rather than mirroring storage. |

### `CTRL.BLOCK_ENABLE` — `0x2000`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `PFB` | RW | `0x1` | — | Polyphase filter bank (issue #11). |
| `1` | `FFT` | RW | `0x1` | — | FFT (issue #10). |
| `2` | `BEAMFORMER` | RW | `0x1` | — | Beamformer (issue #12). |
| `3` | `COVARIANCE` | RW | `0x1` | — | Covariance (issue #13). |
| `4` | `CFAR` | RW | `0x1` | — | CFAR detector (issue #14). |
| `5` | `PACKET` | RW | `0x1` | — | Packet fabric (issue #18). |
| `6` | `MEMORY` | RW | `0x1` | — | History memory / corner turn (issue #15). |
| `7` | `TELEMETRY` | RW | `0x1` | — | Telemetry and counters (issue #8). |

### `CTRL.BLOCK_RESET` — `0x2004`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `PFB` | RWP | `0x0` | — | Pulse a soft reset at the PFB. |
| `1` | `FFT` | RWP | `0x0` | — | Pulse a soft reset at the FFT. |
| `2` | `BEAMFORMER` | RWP | `0x0` | — | Pulse a soft reset at the beamformer. |
| `3` | `COVARIANCE` | RWP | `0x0` | — | Pulse a soft reset at the covariance engine. |
| `4` | `CFAR` | RWP | `0x0` | — | Pulse a soft reset at the CFAR detector. |
| `5` | `PACKET` | RWP | `0x0` | — | Pulse a soft reset at the packet fabric. |
| `6` | `MEMORY` | RWP | `0x0` | — | Pulse a soft reset at the history memory. |
| `7` | `TELEMETRY` | RWP | `0x0` | — | Pulse a soft reset at the telemetry block. |

### `CTRL.GLOBAL_CTRL` — `0x2008`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `GLOBAL_ENABLE` | RW | `0x1` | — | Master enable for the datapath. |
| `1` | `FLUSH` | RWP | `0x0` | — | Pulse a pipeline flush. |
| `2` | `SOFT_RESET` | RWP | `0x0` | — | Pulse a datapath-wide soft reset. |

### `CTRL.CTRL_STATUS` — `0x200C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `3:0` | `ENABLED_COUNT` | ROHW | `0x0` | — | Population count of BLOCK_ENABLE. |
| `8` | `ALIVE` | ROHW | `0x0` | — | Tied high by the block; reads 1 whenever the control plane is out of reset. |

## `fault` — `0x3000`

Fault injection (SPEC 24). Injection is two-step on purpose: a persistent arming mask plus a one-shot trigger. A single stray write can therefore never inject a fault, and the arming state is visible in a register dump taken after the fact.

| Offset | Address | Register | Access | Reset | Description |
|---|---|---|---|---|---|
| `0x000` | `0x3000` | `FAULT_ENABLE` | RW | `0x00000000` | Arming mask. A trigger bit has no effect unless the matching enable bit is set. |
| `0x004` | `0x3004` | `FAULT_INJECT` | RWP | `0x00000000` | One-shot triggers, same bit assignment as FAULT_ENABLE. Writing 1 to an armed bit emits a one-cycle pulse and sets the matching FAULT_STATUS bit. Always reads 0. |
| `0x008` | `0x3008` | `FAULT_STATUS` | W1C | `0x00000000` | Sticky record of faults actually injected, set by hardware and cleared by writing 1. A hardware set in the same cycle as a software clear wins, so an event is never lost to a racing clear. |
| `0x00C` | `0x300C` | `FAULT_COUNT` | ROHW | `0x00000000` | Free-running count of injected fault pulses (one per armed bit per trigger). Saturating, not wrapping, so a dump taken long after the event still reports 'many' rather than an ambiguous small number. |

### `FAULT.FAULT_ENABLE` — `0x3000`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `STREAM_CORRUPT` | RW | `0x0` | — | Corrupt a stream payload. |
| `1` | `SEQ_ERROR` | RW | `0x0` | — | Force a sequence discontinuity. |
| `2` | `FIFO_OVERFLOW` | RW | `0x0` | — | Force a FIFO overflow (issue #6 primitives). |
| `3` | `CDC_ERROR` | RW | `0x0` | — | Force a CDC error report. |
| `4` | `SATURATION` | RW | `0x0` | — | Force a fixed-point saturation event. |
| `5` | `PACKET_DROP` | RW | `0x0` | — | Drop a packet in the packet fabric. |

### `FAULT.FAULT_INJECT` — `0x3004`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `STREAM_CORRUPT` | RWP | `0x0` | — | Trigger a stream corruption. |
| `1` | `SEQ_ERROR` | RWP | `0x0` | — | Trigger a sequence discontinuity. |
| `2` | `FIFO_OVERFLOW` | RWP | `0x0` | — | Trigger a FIFO overflow. |
| `3` | `CDC_ERROR` | RWP | `0x0` | — | Trigger a CDC error. |
| `4` | `SATURATION` | RWP | `0x0` | — | Trigger a saturation event. |
| `5` | `PACKET_DROP` | RWP | `0x0` | — | Trigger a packet drop. |

### `FAULT.FAULT_STATUS` — `0x3008`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `0` | `STREAM_CORRUPT` | W1C | `0x0` | — | A stream corruption was injected. |
| `1` | `SEQ_ERROR` | W1C | `0x0` | — | A sequence discontinuity was injected. |
| `2` | `FIFO_OVERFLOW` | W1C | `0x0` | — | A FIFO overflow was injected. |
| `3` | `CDC_ERROR` | W1C | `0x0` | — | A CDC error was injected. |
| `4` | `SATURATION` | W1C | `0x0` | — | A saturation event was injected. |
| `5` | `PACKET_DROP` | W1C | `0x0` | — | A packet drop was injected. |

### `FAULT.FAULT_COUNT` — `0x300C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | ROHW | `0x0` | — | Injected fault pulses, saturating at 0xFFFFFFFF. |

## `scratch` — `0x4000`

Software scratch registers with no hardware effect. They exist to test the fabric itself: four distinct reset values prove per-register reset defaults, and SCRATCH3 mixes a writable half with a read-only half so that partial writability is covered by construction rather than by a future block happening to need it. The snapshot and debug-control group proper lands with issue #19 in the debug window.

| Offset | Address | Register | Access | Reset | Description |
|---|---|---|---|---|---|
| `0x000` | `0x4000` | `SCRATCH0` | RW | `0x00000000` | Scratch, reset all zeros. |
| `0x004` | `0x4004` | `SCRATCH1` | RW | `0xFFFFFFFF` | Scratch, reset all ones. |
| `0x008` | `0x4008` | `SCRATCH2` | RW | `0xA5A5A5A5` | Scratch, alternating reset. |
| `0x00C` | `0x400C` | `SCRATCH3` | MIXED | `0xDEAD5A5A` | Half writable, half constant. A write updates RW_LOW and leaves RO_HIGH untouched, and the transaction succeeds (error=0) because the register is partially writable. |

### `SCRATCH.SCRATCH0` — `0x4000`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | RW | `0x0` | — | Free software use. |

### `SCRATCH.SCRATCH1` — `0x4004`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | RW | `0xFFFFFFFF` | — | Free software use. |

### `SCRATCH.SCRATCH2` — `0x4008`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `31:0` | `VALUE` | RW | `0xA5A5A5A5` | — | Free software use. |

### `SCRATCH.SCRATCH3` — `0x400C`

| Bits | Field | Access | Reset | Source | Description |
|---|---|---|---|---|---|
| `15:0` | `RW_LOW` | RW | `0x5A5A` | — | Writable half. |
| `31:16` | `RO_HIGH` | RO | `0xDEAD` | — | Constant half; writes are ignored, not refused. |

## `coeff` — `0x5000`

PLANNED. Coefficient and beam-weight programming with double buffering and an active-bank select. Declared here so the window is reserved and the address space cannot be reassigned by a later issue; unimplemented until its owning issues land, and every access to this window returns error=1 today.

**Planned, not implemented in this build** (owning issues: #10, #11, #12, #16).
The window is reserved and every access to it returns `error=1`.

## `cfar` — `0x6000`

PLANNED. CFAR guard/training geometry and threshold scaling, plus coherent and non-coherent integration settings.

**Planned, not implemented in this build** (owning issues: #14, #16).
The window is reserved and every access to it returns `error=1`.

## `counters` — `0x7000`

PLANNED. Performance and health telemetry. The counters themselves are issue #8; this window is where they are read from. Counters live in the telemetry clock domain, so this block will be the first consumer of the issue #6 CDC primitives on the register path.

**Planned, not implemented in this build** (owning issues: #8).
The window is reserved and every access to it returns `error=1`.

## `debug` — `0x8000`

PLANNED. Snapshot capture, trigger configuration and debug readback.

**Planned, not implemented in this build** (owning issues: #19).
The window is reserved and every access to it returns `error=1`.

## Regenerating

```sh
python3 scripts/gen_regmap.py          # rewrite every artefact
python3 scripts/gen_regmap.py --check  # fail if any artefact is stale or hand edited
make regmap-check                      # the same check, as run by lint and sim-tiny
```
